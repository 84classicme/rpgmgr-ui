import { StripSkillPipe } from './strip-skill.pipe';

describe('StripSkillPipe', () => {
  it('create an instance', () => {
    const pipe = new StripSkillPipe();
    expect(pipe).toBeTruthy();
  });
});
